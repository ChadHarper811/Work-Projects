// Will write out puesdo code to start and work into the real javascript from there


// 1st function will organize current sheets all at once
// Need a function that will work with google sheets 
// Function should loop through all sheets in spreadsheet
// Function should then loop through only columns F & G
// function will then loop through all rows starting at row 4
// function will take each cell content and change to all caps
// function will ignore specific words that should be left in caps and loop through the rest changing to proper case
// function could possibly rearrange content to match my organize method (might not work as intended too many mistakes to fix possibly)
// function will then set the cell content

// 2nd function will do same to cells but will trigger on edit and hopefully only run on the currently active sheet and not need to loop through all sheets
